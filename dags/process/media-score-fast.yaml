name: media-score-fast
description: Score media sources on evidence quality and uncertainty expression (keyword-based, no LLM)
schedule: "45 */4 * * *"
steps:
  - id: score_recent_posts
    shell: /bin/bash
    script: |
      cd /dags
      python3 processors/media_score_fast.py --hours 6
    retry_policy:
      limit: 1
      interval_sec: 120
