name: collect-earthquake
description: USGS seismic events in Baltic/Fennoscandia region
schedule: "0 */12 * * *"
steps:
  - id: collect
    shell: /bin/bash
    script: |
      cd /dags && python3 scripts/collectors/earthquake_collector.py
    retry_policy:
      limit: 2
      interval_sec: 300
