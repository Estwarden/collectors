name: collect-stats-ee
description: Collect Statistics Estonia economic indicators
schedule: "0 9 * * 1"
queue: collectors
steps:
  - id: collect
    shell: /bin/bash
    script: |
      cd /dags && python3 scripts/collectors/stats_ee_collector.py
    retry_policy:
      limit: 2
      interval_sec: 300
