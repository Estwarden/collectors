name: collect-airspace
description: Military NOTAMs and EASA Conflict Zone bulletins
schedule: "0 */6 * * *"
steps:
  - id: collect
    shell: /bin/bash
    script: |
      cd /dags && python3 scripts/collectors/airspace_collector.py
    retry_policy:
      limit: 2
      interval_sec: 300
