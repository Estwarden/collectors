name: collect-ais
description: Collect AIS vessel positions (Baltic Sea)
schedule: "*/5 * * * *"
queue: collectors


steps:
  - id: collect
    shell: /bin/bash
    script: |
      cd /dags
      python3 scripts/collectors/ais_collector.py
    retry_policy:
      limit: 2
      interval_sec: 300
