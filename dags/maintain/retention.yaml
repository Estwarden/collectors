name: data-retention
description: Delete signals older than 180 days (except OSINT/satellite)
schedule: "0 4 * * *"


steps:
  - id: retention_cleanup
    type: http
    command: "POST ${ESTWARDEN_API_URL}/api/v1/maintenance/retention"
    config:
      headers:
        X-Pipeline-Key: "${ESTWARDEN_API_KEY}"
      timeout: 120
    retry_policy:
      limit: 2
      interval_sec: 60
