name: backfill-classify
description: Classify untagged z-channel + Estonian signals via LLM


steps:
  - id: classify
    shell: /bin/bash
    script: |
      cd /dags
      python3 processors/backfill_classify.py 2000
    retry_policy:
      limit: 1
      interval_sec: 60
